# Structural repository projection

Use `context_map` and the run's `context/repository-map.json` for content-hashed observed files and actual language-server symbols. Use `context_navigate` for targeted definitions/references when a server supports them. The structural map is distinct from product ownership and never grants write authority. Tests retain the project's real layout.
