---
description: Review the current candidate in an isolated read-only context
argument-hint: "[job-id]"
---

Review `$ARGUMENTS` or the active run independently. Read protected intent, candidate diff, failure history and fresh raw evidence; assess every required criterion and bound. Never modify production files or approve release. If this worker is assigned `write_contract`, publish its verdict through that tool: transcript JSON is not publication. Otherwise return verdict JSON as an advisory review through the node's declared response channel; do not claim an authoritative verdict was written.
