---
description: Implement the current frozen task with TDD
argument-hint: "[task-or-slice]"
---

Implement `$ARGUMENTS` within the current feature's explicit ownership and protected task bounds. Reuse existing code and language conventions; make the smallest complete change with meaningful regression evidence. Claim paths before edits when required. Create only genuinely needed source/tests, never empty scaffolding. Publish the candidate through the assigned channel; never write protected intent, verdict, release approval, or authoritative verifier receipts.
