---
name: autopilot-full
description: Coordinate local K-π owners without remote merge authority
---

Use at most two background Pi sessions through spawn_background and communicate. One writer only. The parent reads contract files, not transcripts. Finish with DONE and an optional local commit after release approval. Never push, deploy, or merge origin.
