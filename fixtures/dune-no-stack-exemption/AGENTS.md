# Fixture guidance

A `typo` playbook: one misspelling in prose, no capability map. `no-stack` playbooks (typo,
unslop, comment-strip) are the named exemptions.

## Quality gates

```bash
npm test
```

Never push. Change the misspelling and nothing else.
