{
  "job_id": "2026-09-02-dune-second-selected-module",
  "mode": "gated",
  "goal": "Ship one slice: the plan advanced to billing while the contract still names auth",
  "nongoals": [
    "admin roles"
  ],
  "acceptance": [
    {
      "id": "AC-01",
      "statement": "the selected slice ships end to end and npm test exits 0",
      "required": true,
      "check": {
        "kind": "command",
        "cmd": "npm test",
        "expect": {
          "exit": 0
        }
      },
      "bounds": {
        "write_allow": [
          "src/billing/**",
          "test/billing/**"
        ]
      }
    }
  ],
  "constraints": [
    "no new runtime dependency"
  ],
  "quality_gates": [
    "npm test"
  ],
  "ac": {
    "quality": "executable"
  },
  "current_module_id": "auth"
}
