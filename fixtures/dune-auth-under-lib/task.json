{
  "job_id": "2026-09-02-dune-auth-under-lib",
  "mode": "gated",
  "goal": "Ship the auth login slice the plan filed under src/lib/auth",
  "nongoals": [
    "billing"
  ],
  "acceptance": [
    {
      "id": "AC-01",
      "statement": "login() returns a session for the user and npm test exits 0",
      "required": true,
      "check": {
        "kind": "command",
        "cmd": "npm test",
        "expect": {
          "exit": 0
        }
      },
      "bounds": {
        "write_allow": [
          "src/lib/auth/**",
          "test/auth/**"
        ]
      }
    }
  ],
  "constraints": [
    "no new runtime dependency"
  ],
  "quality_gates": [
    "npm test"
  ],
  "ac": {
    "quality": "executable"
  },
  "current_module_id": "auth"
}
