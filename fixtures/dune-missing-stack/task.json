{
  "job_id": "2026-09-02-dune-missing-stack",
  "mode": "gated",
  "goal": "Ship the auth login slice inside src/auth with its test twin in test/auth",
  "nongoals": [
    "billing"
  ],
  "acceptance": [
    {
      "id": "AC-01",
      "statement": "login() returns a session for the user and npm test exits 0",
      "required": true,
      "check": {
        "kind": "command",
        "cmd": "npm test",
        "expect": {
          "exit": 0
        }
      },
      "bounds": {
        "write_allow": [
          "src/auth/**",
          "test/auth/**"
        ]
      }
    }
  ],
  "constraints": [
    "no new runtime dependency"
  ],
  "quality_gates": [
    "npm test"
  ],
  "ac": {
    "quality": "executable"
  },
  "current_module_id": "auth"
}
