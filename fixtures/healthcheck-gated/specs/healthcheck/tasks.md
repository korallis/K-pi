# Healthcheck tasks

1. Run the focused test and capture the failing `404` result.
2. Implement the `/health` response in `src/server.js`.
3. Run `npm test` and `npm run lint`.
4. Review the diff against the frozen requirements.
